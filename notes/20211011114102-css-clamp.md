# `clamp()` in CSS

`clamp()` clamps a value between an upper and lower bound. Like `min` and `max` combined.

`clamp(MIN, VAL, MAX)`

It's functionally equivalent to `max(MIN, min(VAL, MAX))`

[[css]]
[[intrinsicdesign]]