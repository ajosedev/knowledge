# View published NPM versions of a package

In general, `npm view` can be used to get information on a package. Appending `versions` gives you a chronological list of all versions.

```sh
npm view <package> versions
```

[[npm]]