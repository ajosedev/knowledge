# Copilot Fleet

The `/fleet` slash command lets Copilot CLI break down a complex request into smaller tasks and run them in parallel across different agents, maximizing efficiency and throughput.

Claude has something similar with 'Agent Teams'. All of these tools are ways of co-ordinating multiple agents to finish a task more efficiently, with one agent acting as the main coordinator.

[[ai]]