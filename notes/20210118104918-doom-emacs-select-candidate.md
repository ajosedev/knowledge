# Open file/search/etc entry with keyboard shortcut in Doom

When searching for files, opening a file, etc. using `C-'` will show 'marks' for opening a entry without having to scroll and use the enter key.

[[emacs]]